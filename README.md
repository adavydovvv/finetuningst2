# 🖥️ Классификация компьютерных устройств

## 📊 Обзор проекта

Проект демонстрирует полный цикл разработки системы классификации изображений компьютерных устройств с использованием методов глубокого обучения. Основная задача - классификация изображений на три класса: **клавиатуры**, **мыши** и **звуковые карты**.

### 🎯 Ключевые особенности:
- **Fine-tuning** предварительно обученных моделей (ResNet18, EfficientNet-B0)
- **Transfer Learning** с оптимальной стратегией замораживания
- **Аугментация данных** для повышения обобщающей способности
- **Гиперпараметрическая оптимизация** с логированием результатов
- **Экспорт в ONNX** для эффективного инференса на CPU
- **Веб-приложение** на Gradio для демонстрации

### 🏆 Лучший результат:
**EfficientNet-B0** с точностью **96.67%** на валидационном наборе

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Клонирование репозитория
git clone <repository-url>
cd fine-tuning

# Создание виртуального окружения
python -m venv .venv

# Активация окружения
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Структура данных

```
data/raw/
├── train/
│   ├── keyboard/    (32 изображения)
│   ├── mouse/       (32 изображения)
│   └── soundcard/   (32 изображения)
└── val/
    ├── keyboard/    (10 изображений)
    ├── mouse/       (10 изображений)
    └── soundcard/   (10 изображений)
```

**Общее количество:** 96 изображений (66 для обучения, 30 для валидации)

### 3. Обучение модели

```bash
# Обучение лучшей модели (EfficientNet-B0)
python experiments/train.py --model_name efficientnet_b0 --epochs 12

# Обучение ResNet18 для сравнения
python experiments/train.py --model_name resnet18 --epochs 12

```

### 4. Экспорт в ONNX

```bash
# Экспорт лучшей модели
python export_onnx.py

# Экспорт конкретной модели
python export_onnx.py --model_name resnet18

```

### 5. Запуск веб-приложения

```bash
# Полное приложение с примерами
python app/gradio_app.py
```

**Приложение доступно по адресу:** http://127.0.0.1:7860

---

## 📈 Результаты экспериментов

| Модель | Семейство | Точность | Параметры | Время обучения |
|--------|-----------|----------|-----------|----------------|
| ResNet18 | ResNet | 93.33% | 11.2M | ~10 мин |
| **EfficientNet-B0** | **EfficientNet** | **96.67%** | **4.0M** | **~15 мин** |

### 📊 Детальный анализ:
- **Лучшая эпоха:** 2 (EfficientNet-B0)
- **Стратегия замораживания:** 5 эпох backbone + 7 эпох полное обучение
- **Оптимизатор:** AdamW с StepLR
- **Аугментации:** RandomResizedCrop, HorizontalFlip, Rotate, ColorJitter

---

## 🔧 Технические детали

### Оптимальные гиперпараметры (EfficientNet-B0):
```python
{
    "model_name": "efficientnet_b0",
    "image_size": 224,
    "batch_size": 16,
    "epochs": 12,
    "learning_rate": 1e-3,
    "weight_decay": 1e-4,
    "optimizer": "AdamW",
    "scheduler": "StepLR",
    "step_size": 5,
    "gamma": 0.1,
    "freeze_backbone": True,
    "unfreeze_at_epoch": 5
}
```

### Стратегия замораживания:
1. **Эпохи 1-5:** Заморожен backbone, обучается только классификатор
2. **Эпоха 5:** Размораживание backbone с уменьшением LR в 10 раз
3. **Эпохи 6-12:** Обучение всей модели

### Аугментации данных:
```python
# Обучающие аугментации
A.Compose([
    A.RandomResizedCrop(224, 224, scale=(0.8, 1.0)),
    A.HorizontalFlip(p=0.5),
    A.Rotate(limit=20, p=0.3),
    A.ColorJitter(brightness=0.2, contrast=0.2, p=0.3),
    A.Normalize(mean=(0.485, 0.456, 0.406), std=(0.229, 0.224, 0.225))
])

# Валидационная предобработка
A.Compose([
    A.Resize(224, 224),
    A.Normalize(mean=(0.485, 0.456, 0.406), std=(0.229, 0.224, 0.225))
])
```

---

## 📁 Структура проекта

```
fine-tuning/
├── 📁 app/                          # Веб-приложения
│   ├── gradio_app.py               # Полное приложение с примерами
│   └── simple_gradio_app.py        # Простое приложение
├── 📁 data/                        # Данные
│   └── raw/
│       ├── train/                  # Обучающие данные
│       └── val/                    # Валидационные данные
├── 📁 experiments/                 # Эксперименты и обучение
│   ├── notebooks/
│   │   └── comprehensive_experiments.ipynb  # Jupyter ноутбук
│   ├── configs.py                 # Конфигурации моделей
│   ├── datasets.py                # Обработка данных
│   ├── train.py                   # Скрипт обучения
│   ├── utils.py                   # Утилиты
│   ├── hyperparameter_tuning.py   # Подбор гиперпараметров
│   ├── freezing_strategies.py     # Анализ стратегий замораживания
│   └── logs/                      # Логи и метрики
├── 📁 models/                      # Обученные модели
│   ├── best_efficientnet_b0.pth   # Веса лучшей модели
│   └── efficientnet_b0.onnx       # ONNX модель
├── export_onnx.py                 # Экспорт в ONNX
├── run_experiments.py             # Запуск экспериментов
├── check_environment.py           # Проверка окружения
└── requirements.txt               # Зависимости
```


### Jupyter ноутбук:
```bash
jupyter notebook experiments/notebooks/comprehensive_experiments.ipynb
```

## 📊 Анализ и визуализация

### Jupyter ноутбук включает:
- **Загрузку и анализ данных**
- **Создание датасетов с аугментациями**
- **Обучение и валидацию моделей**
- **Сравнение архитектур**
- **Построение кривых обучения**
- **Генерацию матриц ошибок**
- **Анализ результатов**

### Расширенные эксперименты:
```bash
# Подбор гиперпараметров
python experiments/hyperparameter_tuning.py

# Анализ стратегий замораживания
python experiments/freezing_strategies.py
```
---

## 🔍 Предобработка данных

```python
# Валидационная предобработка (обучение)
A.Compose([
    A.Resize(224, 224),
    A.Normalize(mean=(0.485, 0.456, 0.406), std=(0.229, 0.224, 0.225))
])

```

---

## 🚀 Производительность

### Метрики лучшей модели:
- **Точность:** 96.67% на валидационном наборе
- **Размер модели:** 4.0M параметров
- **Время инференса:** ~50ms на CPU
- **Размер ONNX:** ~15MB
- **Время обучения:** ~15 минут

### Системные требования:
- **Python:** 3.8+
- **PyTorch:** 2.0+
- **RAM:** 8GB+ (рекомендуется)
- **GPU:** CUDA-совместимая (опционально)

---

## 🛠️ Разработка и расширение

### Добавление новых классов:
1. Создайте папки в `data/raw/train/` и `data/raw/val/`
2. Добавьте изображения в соответствующие папки
3. Обновите `num_classes` в конфигурации
4. Переобучите модель

### Кастомизация аугментаций:
Отредактируйте функцию `get_transforms()` в `experiments/datasets.py`

---